{
  "name": "my-bomz-bot",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply my-bomz-bot --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.134.0"
  }
}
